<%= form_with(model: user, local: true) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>


  <%= f.label :avatar_path  %>
<form>
  <%= f.file_field :avatar_path, id: :user_avatar_path ,accept: "image/*" %>

<%= image_tag @user.avatar_path.url, id: :img_prev if @user.avatar_path? %>

<div class="preview" />
</div>

  <div class="actions">
    <%= f.submit %>
  </div>
</form>

<% end %>
